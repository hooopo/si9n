<% title "我的语录" %>
<h1>我添加的语录</h1>
<% if @signatures.present?  %>
  <%= will_paginate(@signatures, :class => "digg_pagination") %>
  <%= render :partial => "signature", :collection => @signatures, :as => :signature %>
  <%= will_paginate(@signatures, :class => "digg_pagination") %>
<% end %>